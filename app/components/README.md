# Components

Componentes React reutilizables organizados por categoría.

## 📁 Estructura

```
components/
├── layout/          # Componentes de estructura y layout
│   ├── Logo.tsx     # Logo de la aplicación
│   └── ThemeToggle.tsx  # Switch de tema
│
└── ui/              # Componentes de interfaz de usuario
    ├── AnimatedText.tsx  # Texto con animación
    └── InputField.tsx    # Campo de entrada con botón
```

## 📝 Convenciones

- **layout/**: Componentes relacionados con la estructura de la página (header, footer, navegación)
- **ui/**: Componentes de interfaz reutilizables (botones, inputs, cards, etc.)

